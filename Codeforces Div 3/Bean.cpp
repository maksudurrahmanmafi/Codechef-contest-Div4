#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int a,b,c,d;
    cin>>a>>b>>c>>d;
    if(a==1)cout<<1<<'\n';
    else if (b==1)
    {
        cout<<2<<'\n';
    }
    else if(c==1)cout<<3<<'\n';
    else cout<<4<<'\n';
    
    return 0;
}
