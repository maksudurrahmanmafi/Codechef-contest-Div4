#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int x,y;
    cin>>x>>y;
    if((x+y)%2==0)cout<<1<<'\n';
    else cout<<0<<'\n';
    return 0;
}
